export interface Study {
  title: string;       // Título do estudo
  subject: string;     // Matéria ou disciplina
  duration: number;    // Duração em minutos
  difficulty: number;  // Escala 1-5
  date: string;        // Data do estudo
}